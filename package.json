{
  "name": "ai-assistant",
  "version": "1.0.0",
  "private": true,
  "description": "WhatsApp AI assistant powered by Claude, using Meta Cloud API",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest --runInBand",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "googleapis": "^171.4.0",
    "node-cron": "^4.2.1"
  },
  "devDependencies": {
    "jest": "^30.2.0"
  }
}
